# NPM Registry Configuration for npmjs.com
registry=https://registry.npmjs.org/

# 设置为公开发布（对于公共包）
access=public

# 发布时自动创建标签
tag-version-prefix=""

# 保存确切的版本号
save-exact=true

# 使用 npm 官方源进行安装
@*:registry=https://registry.npmjs.org/

# 启用包锁文件
package-lock=true

# 发布时显示详细信息
loglevel=info
